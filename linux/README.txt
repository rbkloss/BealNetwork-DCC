Para execução, rode primeiro o Servidor com:
  Servidor.exe porta
Então para calcular um intervalo, rode o cliente da seguinte forma:
  Cliente.exe hostAddr port idCliente
Para consultar o status, do browser digite hostAddr:port

Caso o makefile padrão não funcione, tente a cópia.(Pode ser problam com o -std)